{
  "name": "test-doubles-typescript-sample",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc && vite build",
    "lint": "biome check ./src",
    "lint-fix": "biome check --write ./src --diagnostic-level=error",
    "preview": "vite preview",
    "test": "vitest run --logHeapUsage"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/jest": "^29.5.14",
    "typescript": "~5.6.2",
    "vite": "^6.0.5",
    "vitest": "^3.0.5"
  }
}
